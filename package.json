{
  "name": "gameboy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browserify index.js -o bundle.js && http-server",
    "vite": "vite",
    "build": "vite build",
    "prettier": "npx prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.8.8",
    "vite": "^4.3.5"
  },
  "dependencies": {
    "vite-plugin-live-reload": "^3.0.2"
  }
}
